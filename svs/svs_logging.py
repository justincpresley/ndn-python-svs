# Basic Libraries
import logging

#log_file = "SVS.log"
#logging.basicConfig(format='%(asctime)s - %(name)s - %(levelname)s - %(message)s', \
#    filename=log_file, \
#    filemode='w+', \
#    level=logging.INFO)